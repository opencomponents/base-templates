{
  "devDependencies": {
    "husky": "^0.13.4",
    "jest": "^20.0.4",
    "lerna": "^2.0.0-rc.5",
    "lint-staged": "^3.6.0",
    "prettier": "^1.4.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "precommit": "lint-staged",
    "test": "jest"
  },
  "lint-staged": {
    "*.js": [
      "prettier --single-quote --write",
      "git add"
    ]
  }
}
